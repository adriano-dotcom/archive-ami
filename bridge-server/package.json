{
  "name": "archive-ami-bridge-server",
  "version": "1.0.0",
  "description": "WebRTC bridge server for WhatsApp Calling — relays audio between Meta and the agent browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@roamhq/wrtc": "^0.8.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
